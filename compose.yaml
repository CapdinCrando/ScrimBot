services:
  scrimbot:
    image: scrimbot:latest
    container_name: scrimbot
    restart: unless-stopped
    build:
      context: .
    volumes:
      - ${DATA_DIR}/config:/app/config
      - ${DATA_DIR}/resources:/app/resources
